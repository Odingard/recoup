# Recoup Web Frontend

Vite environment variables:

- `VITE_API_BASE` sets the backend API base URL.
- If unset, the app falls back to `http://127.0.0.1:8001/api`.

The frontend supports both Firebase-authenticated sessions and the explicit
"Try with sample data" mode used for the offline demo.
